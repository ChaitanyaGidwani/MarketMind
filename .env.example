USE_MOCK=false

A2A_HUB_URL=http://a2a_hub:8000
ORCHESTRATOR_URL=http://orchestrator:8001
CONTENT_AGENT_URL=http://content_agent:8002
AD_AGENT_URL=http://ad_agent:8003
EMAIL_AGENT_URL=http://email_agent:8004
SEO_AGENT_URL=http://seo_agent:8005
ANALYTICS_AGENT_URL=http://analytics_agent:8006

DATABASE_URL=sqlite+aiosqlite:///./data/marketmind.db
CORS_ORIGINS=*

GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
RESEND_TO_EMAIL=marketing@yourcompany.com
VITE_A2A_HUB_URL=http://localhost:8000

# --- Web3 / Blockchain settings (Sepolia testnet) ---
# Alchemy RPC URL for Sepolia. TODO: insert your Sepolia RPC URL (do not commit secrets).
ALCHEMY_RPC_URL=

# Deployed contract address for CampaignBudget. TODO: fill after deploying contract.
CONTRACT_ADDRESS=

# Alternatively, you can paste the contract ABI JSON string here (not recommended) or set CONTRACT_ABI_PATH
# CONTRACT_ABI=

# Path to a JSON file containing the contract ABI (relative to repo root or absolute). e.g. blockchain/artifacts/CampaignBudget.json
CONTRACT_ABI_PATH=

# Private key for orchestrator wallet (used to call settleCampaign). TODO: use secure secrets manager; do NOT commit.
ORCHESTRATOR_PRIVATE_KEY=

# Chain ID for Sepolia
CHAIN_ID=11155111

# Blockchain / Web3 (Sepolia)
# Replace with your Alchemy RPC URL for Sepolia (or other provider). TODO: insert your Alchemy RPC URL
ALCHEMY_RPC_URL=

# Deployed CampaignBudget contract address (Sepolia)
CONTRACT_ADDRESS=

# Contract ABI JSON string. For local development you can paste the ABI here or mount a file.
# TODO: paste contract ABI JSON string here when deploying
CONTRACT_ABI=

# Private key for orchestrator wallet (owner) to call settleCampaign. WARNING: keep this secret.
# TODO: use a secrets manager; don't commit real keys.
ORCHESTRATOR_PRIVATE_KEY=

# Sepolia chain id
CHAIN_ID=11155111
